name: Reusable Deploy
on: workflow_call #evento que aciona o workflow, que é um evento personalizado, que é acionado por outro workflow. no caso, é o .github/workflows/using-reuse.yaml
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run: echo "Deploying & uploading..."